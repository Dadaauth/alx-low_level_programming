#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
	char *s;

	s = "I do not fear computers for i am destined to become an inventor!!";
	print_rev(s);
	return (0);
}
