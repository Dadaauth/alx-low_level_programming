#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear the use of computers. I am destined to become an inventor!!";
	_puts(str);
	return (0);
}
