#include <stdio.h>
/**
 * main -short desc
 * Return: returns 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if (c != q && c != e)
		{
			putchar(c);
		}
	}
	putchar('\n');

	return (0);
}
