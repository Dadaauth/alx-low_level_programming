#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 *
 * Return: it returns the result of
 *  the multiplication not just 0 .
 */
int mul(int a, int b)
{
	int multiplication = a * b;

	return (multiplication);
}
