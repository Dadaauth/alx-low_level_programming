#include "main.h"
/**
 * add - adds two intgers
 * @a: first value
 * @b: second value
 * Return: the return value is the addition of the int's
 */
int add(int a, int b)
{
	int add = a + b;

	return (add);
}
