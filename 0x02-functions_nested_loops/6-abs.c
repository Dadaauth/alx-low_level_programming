#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @c: the value to be worked on
 * Return: return value is based on input
 */
int _abs(int c)
{
	/** int rv;
	 */

	if (c < 0)
	{
		return (-c);	
	}
	else
	{
		return (c);
	}

}
